<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Verify OTP</title>

  <!-- Bootstrap CSS -->
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
    rel="stylesheet"
  />

  <!-- Custom OTP Styles -->
  <link rel="stylesheet" href="/css/otp.css" />
</head>
<body>

  <!-- OTP Modal -->
  <div class="modal show d-block" tabindex="-1">
    <div class="modal-dialog modal-dialog-centered">
      <div class="modal-content otp-modal">
        <form action="/enterOtp" method="POST">
          <div class="modal-body text-center">
            <h4 class="otp-title">Verify</h4>
            <p class="otp-subtext">Your code was sent to you via email</p>

            <input type="hidden" name="email" value="<%= email %>" />

            <div class="otp-input-group">
              <% for (let i = 0; i < 6; i++) { %>
                <input type="text" name="otp" maxlength="1" class="otp-box" required />
              <% } %>
            </div>

            <% if (message) { %>
              <div class="text-success mt-2"><%= message %></div>
            <% } %>

            <button type="submit" class="btn btn-verify mt-3">Verify</button>

            <div class="mt-3">
              <a href="/resendOtp" class="resend-link">Didn’t receive code? Request again</a>
            </div>
          </div>
        </form>
      </div>
    </div>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
