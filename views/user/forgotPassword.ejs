<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Forgot Password</title>

  <!-- Bootstrap CSS -->
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
    rel="stylesheet"
  />

  <!-- Custom CSS -->
  <link rel="stylesheet" href="/css/forgot.css" />
</head>
<body>
  <!-- Forgot Password Modal -->
  <div class="modal fade show" id="forgotPasswordModal" tabindex="-1" aria-hidden="true" style="display: block;">
    <div class="modal-dialog modal-dialog-centered">
      <div class="modal-content forgot-modal">
        <form action="/auth/forgotPassword" method="POST">
          <div class="modal-body text-center">
            <h4 class="modal-title mb-3">Forgot Password?</h4>

            <div class="form-group text-start">
              <label class="form-label">Email<span class="text-danger">*</span></label>
              <input type="email" name="email" class="form-control forgot-input" required />
            </div>

            <% if (message) { %>
              <div class="text-primary mt-2"><%= message %></div>
            <% } %>

            <button type="submit" class="btn btn-reset mt-3">Reset Password</button>
            <div class="mt-3">
              <a href="/login" class="back-link">← Back to Log In</a>
            </div>
          </div>
        </form>
      </div>
    </div>
  </div>

  <!-- Bootstrap JS (for modal functionality) -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
